# Notes about camera

- Camera outputs 2d grid of pixel values
- Represented by a Matrix: height x width x channels
    - height: number of rows
    - width: number of cols
    - channels: (Red, Green, Blue)m, between 0 and 255 [R, G, B]

- Image is a numerical array of pixel values we can process